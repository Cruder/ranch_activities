console.log("Relative");

$("tr[data-id='<%= @activity.id %>'] > td.people").each(function(index) {
    <% if @activity.people >= 0 %>
      value = $(this)
      value.text(<%= @activity.people %>)
    <% end %>
});
